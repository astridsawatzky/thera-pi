# project specific settings

# For file names of JAR files created from source use the
# properties from build-common.properties instead of literal file names.

# JAR file name.
file.output.jar=${file.jar.reha}

# main class
jar.main.class=hauptFenster.Reha

# dependencies from JAR files created by other projects (separated by space)
jar.classpath.jars=${file.jar.reha.common} ${file.jar.nebraska} ${file.jar.therapi.updates} ${file.jar.arzt.baustein} ${file.jar.text.baustein} ${file.jar.icd.suche} ${file.jar.op.rgaf} ${file.jar.offene.posten} ${file.jar.smartcard} ${file.jar.therapi.database} ${file.jar.calcKilometer}
projektname=Reha
